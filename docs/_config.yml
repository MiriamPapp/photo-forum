theme: jekyll-theme-cayman

# Site settings
title: Photo forum software for AWS
email: frank@tegtmeyer.net
description: > # this means to ignore newlines until "baseurl:"
  Photo-forum is a set of configuration and softwware whicnh allows
  everyone to host a forum which acts like a Google+ comunity.
  It is designed to be used by groups of photographers but is not
  limited to this. The goal is to make hosting extremely cheap by
  using only base services of Amazon Web Services (AWS).
baseurl: "" # the subpath of your site, e.g. /blog/
url: "https://fte378.github.io/photo-forum/"
github_username:  jekyll

# Build settings
# Build Settings
sass:
  sass_dir: _sass
include: ['_pages']
markdown: kramdown
kramdown:
  input: GFM